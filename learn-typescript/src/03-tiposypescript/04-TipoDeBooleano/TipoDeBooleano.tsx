let pendiente: boolean;

pendiente = true;

console.log(pendiente);

pendiente = false;

console.log(pendiente);

pendiente = "hola"; //  El tipo 'string' no se puede asignar al tipo 'boolean'.

export const TipoDeBooleano = () => {
  return <div>TipoDeBooleano</div>;
};
